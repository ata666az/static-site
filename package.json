{
  "name": "static-vpn-proxy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "preview": "npx serve public",
    "deploy": "wrangler pages deploy public"
  }
}
